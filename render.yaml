services:
  - type: web
    name: drip-poker
    runtime: node
    plan: free
    buildCommand: npm install && npm install --prefix client && npm run build
    startCommand: npm start
    envVars:
      - key: HOST
        value: 0.0.0.0
